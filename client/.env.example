# Client environment variables (create a .env.local in client/ for development)
# Vite uses VITE_ prefix for env variables exposed to the client

# URL of the backend API (include /api if you want). Example:
 #VITE_API_URL=https://timetable-backend.onrender.com/api

# Example for development (optional):
# VITE_API_URL=http://localhost:8000/api

# Other client-only flags
# VITE_SENTRY_DSN=
# VITE_APP_NAME=TimeTableGeneratorClient
VITE_API_URL=http://localhost:8000/api
VITE_APP_NAME=TimeTableGeneratorClient
